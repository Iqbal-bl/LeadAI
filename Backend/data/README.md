# data/

Files kept from earlier work that no running code reads. Kept, not deleted, in case
someone still needs them.

- `legacy_samples/` â€” sample claim data, knowledge-base JSON and a prebuilt index
- `legacy_binaries/` â€” Windows-only binaries (`RNNOISESHARE.dll`, a Python 3.10 `.pyd`);
  nothing imports them and the Linux Docker image cannot load them
